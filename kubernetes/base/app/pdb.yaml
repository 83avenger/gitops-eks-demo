# PodDisruptionBudget — ensures availability during node drains and upgrades
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: sample-app
spec:
  minAvailable: 1    # Always keep at least 1 pod running
  selector:
    matchLabels:
      app.kubernetes.io/name: sample-app
